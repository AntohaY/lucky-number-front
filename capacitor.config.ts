import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lucky-number-front',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
